# yaml-language-server: $schema=https://schema.blue-build.org/module-v1.json
---
# Laptop power management

type: rpm-ostree
install:
  # Power profiles (GNOME/KDE integrate with this)
  - power-profiles-daemon
  
  # ACPI
  - acpid
  
  # TLP alternative (comment out power-profiles-daemon if using TLP)
  # - tlp
  # - tlp-rdw
